let module = require("valorantinfo");
function getagent(agent) { 
    return agent.id === 'omen';
  };
let tutorial = modül.find(getagent)
console.log(tutorial.bio)
 //A phantom of a memory, Omen hunts in the shadows. He renders enemies blind, teleports across the field, ithen lets paranoia take hold as his foe scrambles to learn where he might strike next.
